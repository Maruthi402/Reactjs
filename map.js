// a map is key value pair keys can any datatype

// new Map(),get, delete(), foreach(), set(), has(), entrinace()
//size number of elements

const fruits = new Map([
    ["Apples", 500],
    ["Banana", 600],
]);

const fruits1 = fruits.entries();

document.write(fruits1.next().value);






